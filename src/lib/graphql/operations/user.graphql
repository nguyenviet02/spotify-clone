query Query {
  me {
    _id
    displayName
  }
}

query Users {
  users {
    _id
    displayName
  }
}

mutation User_register($input: UserRegisterInput) {
  register(input: $input) {
    refreshToken
    token
  }
}
